import { OverlayDirective } from './overlay.directive';

describe('OverlayDirective', () => {
  it('should create an instance', () => {
    const directive = null; //new OverlayDirective();
    expect(directive).toBeTruthy();
  });
});
